# Core
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0
python-dotenv>=1.0.0
requests>=2.31.0

# LLM (Groq - free tier)
groq>=0.9.0

# Embeddings (local, no API needed - PRECOMPUTED)
# sentence-transformers removed to save memory on Render (512MB limit)
numpy>=1.24.0

# Vector Store
faiss-cpu>=1.7.4

# Testing
pytest>=8.0.0
httpx>=0.27.0
